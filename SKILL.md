---
name: dnd-replay
description: "Use when reconstructing D&D/TTRPG session transcripts, chat logs, combat logs, notes, character data, or module references into accurate session recaps and persistent campaign chronicles; also for continuity checks and campaign-history queries. 用于忠实整理跑团战报、维护长期战役状态与查询历史，不用于虚构玩家未经历的剧情。"
---

# D&D 跑团战报与冒险编年史

把原始跑团资料整理成准确、好读、可追溯、可持续更新的冒险档案。核心任务是还原玩家实际经历过的内容，而不是把缺口补成一个看似合理的故事。

每回处理包含两个同等重要但用途不同的成品：

- **读者向战报**：让玩家能够重新经历本回的场景、选择、笑点、危险与情绪转折。
- **档案向状态**：用 Campaign Profile 和 Campaign Ledger 保存可查询、可核对的长期事实。

不要把 Ledger 的数据库写法搬进战报正文。事实审计服务于准确性，不能吞没叙事节奏。

## 事实优先级

按以下顺序判断可信度：

1. 实际跑团记录
2. 用户明确说明
3. 已有 Campaign 历史档案
4. 用户提供的模组资料
5. D&D 通用知识
6. 模型推测

在分析和输出中严格区分：

- **已确认事实**：转写、日志或用户说明明确支持。
- **玩家推测**：玩家或角色提出，但尚未被剧情证实。
- **无法确定**：说话人、名称、数字、行动、结果或归属存在歧义。
- **模组背景**：用于理解世界和专有名词，但不代表玩家已经经历或知晓。

不得编造说话人、NPC、骰点、战斗结果、谜题答案、玩家决定、奖励或物品归属。不得用模组原文替代本团记录。任何推断都要保留“可能 / 推测 / 无法确认”等标记。

## 根据任务选择流程

### 首次建立战役档案

先检查用户当前提供的全部资料，再只询问会实质影响整理结果的缺失信息：

- 模组或 Campaign 名称，以及长模组、短模组或单回冒险；
- 当前是第几回、第几幕或第几章；
- 实际玩家角色名单：玩家名、角色名、种族、职业/子职业、等级和重要背景（已知多少记录多少）；
- 转写的说话人标注情况；必要时确认由模型自动推断，还是由用户提供“说话人 → 玩家/角色”映射。
- 是否需要 Markdown 之外的附加导出：Word (`.docx`)、PDF (`.pdf`) 或纯文本 (`.txt`)。每回战报的 Markdown 主档始终生成，不属于可取消的导出选项。

模组简介、DM 信息和额外世界设定是可选项。用户已经提供的信息不得重复询问。生成每回战报文件时默认只生成 Markdown；首次建档可以顺带问一句：“Markdown 主档会固定生成；是否还需要 Word、PDF 或 TXT？”未回答时按“无附加导出”继续，不得因此阻塞战报整理。

Markdown 是可持续维护的唯一内容主档，生成战报文件时必须写入 `sessions/session-NNN.md`。用户要求 Word、PDF 或 TXT 时，把它们视为 Markdown 之外的附加导出；即使用户说“只要 PDF”，仍保留 Markdown 主档。附加导出必须来自同一份已核对内容，不得另写一版。将附加导出偏好写入 Campaign Profile，后续回合默认沿用，除非用户本回另有要求。

建立档案或维护长期状态时，读取 [references/campaign-state.md](references/campaign-state.md)。

### 整理新一回战报

1. 先找到并读取 Campaign Profile；长模组还要先读取当前 Campaign Ledger。
2. 判断资料类型和说话人标注质量。存在混合音轨、通用标签或重要歧义时，读取 [references/transcript-analysis.md](references/transcript-analysis.md)。
3. 先建立内部事件时间线与证据表，再写正文；不要一边读一边机械摘要。
4. 与此前状态逐项对照。发现冲突时公开标记，不得静默改写旧记录。
5. 读取 [references/chronicle-style.md](references/chronicle-style.md)，先确定本回的叙事主线、场景切分、角色高光、重要台词和能够由事实支持的开头—结尾呼应。
6. 按 [references/report-format.md](references/report-format.md) 生成读者向战报。默认输出完整编年史；只有用户明确要求时才压缩为简报。
7. 读取 [references/output-formats.md](references/output-formats.md)，固定生成 Markdown 主档；只在用户要求或 Profile 已记录偏好时附加生成 Word、PDF 或 TXT。不得只改扩展名，各格式要使用适合各自媒介的结构。
8. 对战报做一次“事实与阅读体验”双重检查，并对 Word/PDF 做视觉验证，再只用有来源支持的事实更新 Campaign Ledger；冲突和不确定项继续保留，等待用户确认。

长模组默认持续维护状态，除非用户明确只要一次性输出。短模组或单回冒险可以使用精简状态；用户要求时再维护完整 Ledger。

### 查询 Campaign 历史

先用 Campaign Ledger 定位，再查 Campaign Profile 和相关回合战报。回答时注明支持结论的回合或档案文件，并区分已确认事实、玩家推测和未解决冲突。记录彼此矛盾时，展示矛盾，不得猜测哪个版本正确。

## 说话人与转写保护规则

转写只有一个说话人标签时，不得将这个标签视为某一名玩家。它可能是把 DM、所有玩家、NPC 台词和规则讨论合并后的混合音轨。

对于“说话人 1/2/3”或单一混合标签，只能根据角色名、称呼、第一人称行动、角色专属能力、知识范围和前后连续性保守推断。如果身份不确定会影响角色行动、关键决定或物品归属，应先让用户选择：自动根据上下文判断，或提供映射表。用户选择自动判断后可以继续，但必须在战报中披露身份推断和遗留歧义。

上下文与本团资料共同指向唯一高置信度 D&D 术语或专有名词时，可以纠正明显的语音识别错误；仅凭模组常识或官方译名不得静默替换本团实际说法。存在多个合理答案时保留原词并标记不确定。记录本团实际采用的 DM 判定和 house rule，不得用标准规则覆盖已经发生的结果。

## 连续性检查

更新长模组状态前，至少核对：

- 每名实际 PC 的等级、状态、装备、金钱、关系和位置；
- NPC 的身份、关系、存亡状态和最后出现回合；
- 线索是否重复、被印证、被否定、出现冲突或已解决；
- 旧谜团是否解决、仍待追查，以及本回新增谜团；
- 队伍从哪里来到当前地点，下一步准备去哪里。

发生矛盾时加入醒目的 `⚠️ 连续性冲突` 区块，写清两条冲突记录、各自来源、仅作为可能性的解释，以及需要用户确认的问题。在用户确认前，不得修改旧结论或把新记录升级为最终事实。

## 语言与输出要求

输出语言按以下优先级决定：用户明确指定的语言 > 用户当前请求所用语言 > 转写的主要语言。资料明显混合且无法判断时才询问。首次建档后把 Campaign 的主要语言写入 Profile；后续回合保持一致，除非用户明确要求切换。专有名词沿用本团写法，重要原话默认保留原语言；需要翻译时标记为译文，不得把译文伪装成逐字原话。

中文 Campaign 使用中文栏目；英文 Campaign 使用 [references/report-format.md](references/report-format.md) 中的英文栏目映射和自然英文叙事。Skill 内部说明使用中文不会要求英文转写先翻译成中文，也不得因为说明语言而降低英文名称、引语、规则术语或语气的保真度。

战报应像玩家愿意重读的冒险者日志或战役编年史，而不是会议纪要或审计报告；但事实准确性永远优先于文学性。

默认使用符合输出语言的第二人称复数重现桌上经历：中文用“你们”，英文自然使用 “you” 或 “the party”。按地点、目标、冲突或情绪转折充分切分场景。保留有意义的原话、玩家选择、失败、后果、桌上笑点和角色高光；技术性时间戳、来源异文和大量不确定说明应移入简短整理说明或 Ledger，不得压在剧情开头。

固定八个栏目都要输出；无内容时使用当前语言简洁写明 `本回无 / None this session`、`未记录 / Not recorded` 或 `无法确认 / Uncertain`。角色栏目必须以 Campaign Profile 中的实际 PC 名单为准：有几名 PC 就写几名，既不补齐固定人数，也不漏掉没有造成伤害的角色。完整战报默认以一段有事实依据的结语收束；不得为了制造主题而发明人物动机或剧情。

文件交付完成后，必须在回复结尾先列出 Markdown 主档的准确路径，再列出实际生成的 Word、PDF 或 TXT。明确说明 Markdown 是必备内容主档，也是最稳定、最适合核对与长期维护的推荐格式；Word/PDF 因字体、分页和转换引擎差异，偶尔会出现轻微版式偏差。不得列出没有生成的格式，也不得只说“已生成”而不告诉用户文件在哪里。
