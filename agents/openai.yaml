interface:
  display_name: "dnd-replay"
  short_description: "Accurate TTRPG chronicles with persistent campaign continuity"
  default_prompt: "Use $dnd-replay to reconstruct this session as an accurate campaign chronicle and update its persistent continuity ledger."
